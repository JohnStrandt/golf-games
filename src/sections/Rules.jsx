import { Nav } from "./index";

const Rules = () => {
  return (
    <section className="flex flex-col p-2 h-[calc(100vh-80px)] justify-around bg-background text-primary">
      <p className="flex mx-auto">Rules</p>
    </section>
  );
};

export default Rules;
